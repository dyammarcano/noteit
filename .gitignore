/target
/.scripts

# Worktrees live inside the checkout; never track their contents.
/.claude/worktrees/

# harden scratch
docs/analysis/.harden/

# internal orchestration reports (not shipped)
/.superpowers/
